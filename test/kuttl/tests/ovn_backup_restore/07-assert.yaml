apiVersion: kuttl.dev/v1beta1
kind: TestAssert
commands:
  - script: |
      oc exec -n $NAMESPACE ovsdbserver-nb-0 -- ovn-nbctl ls-list | grep -q kuttl-test-switch
      if [ $? -ne 0 ]; then
        echo "Test data 'kuttl-test-switch' not found after restore"
        exit 1
      fi
      echo "Test data verified successfully after restore"
      exit 0
